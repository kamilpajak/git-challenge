package step_definitions;

import cucumber.api.java8.En;

public class GitChallengeStepDefinitions implements En {

    public GitChallengeStepDefinitions() {

        this.Given("^an open browser with login page$", () -> {
        });
    }
}
